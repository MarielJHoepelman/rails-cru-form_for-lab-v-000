<h1>Create Song</h1>
<%= form_for(@song) do |f| %>
<%= f.label :name %>
<% @artists.each do |artist| %>
  <label><%= artist.name %></label><input id="<%= artist.name %>" type="radio" name="genre" value="<%= genre.id %>">
<% end %>
<%= f.submit %>
<% end %>
